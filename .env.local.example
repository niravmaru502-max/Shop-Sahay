# Example .env.local for local development
# Set your spreadsheet ID (the long string in the sheet URL between /d/ and /edit)
GOOGLE_SHEETS_ID=YOUR_SPREADSHEET_ID_HERE

# Option A: Paste the full service account JSON as a single-line string (escape newlines in private_key as \n)
# GOOGLE_SERVICE_ACCOUNT='{"type":"service_account","project_id":"...","private_key":"-----BEGIN PRIVATE KEY-----\nMIIE...\n-----END PRIVATE KEY-----\n","client_email":"...@...iam.gserviceaccount.com",... }'

# Option B (recommended): base64-encode the JSON and set this env var instead
# This avoids newline escaping issues.
GOOGLE_SERVICE_ACCOUNT_B64=BASE64_ENCODED_SERVICE_ACCOUNT_JSON

# Example: in PowerShell you can create the base64 string with the helper in scripts/encode_service_account.ps1
